class OnBoard {
  final String image, title, description, background;

  OnBoard({
    required this.image,
    required this.title,
    required this.description,
    required this.background,
  });
}
